function Hnm_mpi = pinv_pm(Hmn)
% this routine calculates the Moore Penrose
% pseudo inverse for an MxN matrix a column at a time
dimension = size(Hmn);
nrow =dimension(1);
ncol =dimension(2);

for mm = 1:nrow
    for nn = 1:ncol
    end
end