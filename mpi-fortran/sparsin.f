	subroutine SPARSIN(A, thresh, sa, ija)
!   sparse matrix creation
!   
!
!
	integer nn,nmax
        dimension sa(nn), ija(nn), A(nn,nn)
	do j = 1,nn

	do i = 1,nn
	  do k = 1,nn
             if 
	  enddo
	enddo

	enddo
	end subroutine SPARSIN
